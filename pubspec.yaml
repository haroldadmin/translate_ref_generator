name: translate_ref_generator
description: A generator for translation string references in Dart
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  build: ^1.5.0

dev_dependencies:
  build_config: ^0.4.2
  build_runner: ^1.10.4
  pedantic: ^1.9.0
  test: ^1.14.4
